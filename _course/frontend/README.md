# Приложение для работы с подкастами

Фронтенд часть приложения для управления подкастами, разработанная на React. Интерфейс выполнен в стиле Яндекс Музыки - простой, красивый и понятный.

## Возможности

- 📱 Просмотр списка подкастов
- 🎧 Детальная информация о подкасте
- 📻 Список эпизодов подкаста
- ▶️ Проигрыватель эпизодов
- 🎨 Современный UI в темной теме
- 📱 Адаптивный дизайн

## Технологии

- **React 18** - библиотека для создания пользовательского интерфейса
- **Vite** - инструмент сборки и разработки
- **CSS3** - стилизация компонентов

## Установка и запуск

1. Установите зависимости:
```bash
npm install
```

2. Запустите приложение в режиме разработки:
```bash
npm run dev
```

3. Откройте браузер по адресу `http://localhost:3000`

## Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут находиться в папке `dist`.

## Структура проекта

```
src/
├── components/          # React компоненты
│   ├── Header/         # Шапка приложения
│   ├── Sidebar/        # Боковая панель навигации
│   ├── PodcastList/    # Список подкастов
│   ├── PodcastCard/    # Карточка подкаста
│   ├── PodcastDetails/ # Детальная страница подкаста
│   ├── EpisodeList/    # Список эпизодов
│   ├── EpisodeItem/    # Элемент списка эпизодов
│   └── Player/         # Проигрыватель
├── data/               # Моковые данные
│   └── mockData.js
├── services/           # API сервисы
│   └── api.js
├── App.jsx            # Главный компонент
├── App.css            # Стили приложения
├── main.jsx           # Точка входа
└── index.css          # Глобальные стили
```

## Интеграция с бэкендом

В файле `src/services/api.js` подготовлены методы для работы с API. Для интеграции с Go бэкендом:

1. Установите переменную окружения `REACT_APP_API_URL` с адресом вашего API
2. Раскомментируйте реальные API вызовы в методах `podcastAPI`
3. Убедитесь, что ваш бэкенд предоставляет следующие endpoints:
   - `GET /api/podcasts` - получение списка подкастов
   - `GET /api/podcasts/:id` - получение подкаста по ID
   - `GET /api/podcasts/:id/episodes` - получение эпизодов подкаста
   - `GET /api/podcasts/search?q=query` - поиск подкастов
   - `POST /api/podcasts` - создание подкаста
   - `PUT /api/podcasts/:id` - обновление подкаста
   - `DELETE /api/podcasts/:id` - удаление подкаста

## Особенности интерфейса

- **Темная тема** - приятная для глаз цветовая схема
- **Адаптивный дизайн** - работает на разных размерах экранов
- **Плавные анимации** - улучшают пользовательский опыт
- **Интуитивная навигация** - понятная структура и переходы

## Будущие улучшения

- [ ] Интеграция с Go бэкендом
- [ ] Реальное воспроизведение аудио
- [ ] Поиск подкастов
- [ ] Избранное и плейлисты
- [ ] Загрузка эпизодов для офлайн прослушивания
- [ ] Авторизация пользователей
- [ ] Подписки на подкасты

## Лицензия

MIT

